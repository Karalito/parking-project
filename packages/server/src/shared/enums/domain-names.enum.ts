export enum DOMAIN_NAMES {
  USERS = 'users',
  PARKING_RESERVATIONS = 'parking-reservations',
  ROOM_RESERVATIONS = 'room-reservations',
  PARKING_SPACES = 'parking-spaces',
  ROOM_SPACES = 'room-spaces',
  POPULATE_DB = 'populate-db',
  HARDWARE = 'hardware',
  TABLE = 'tables',
}
